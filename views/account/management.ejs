<!-- Title View -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<% if (messages && messages.length > 0) { %>
    <div class="alert alert-info">
      <%= messages %>
    </div>
  <% } %>

<div>
    <h1>You're logged in</h1>
</div>